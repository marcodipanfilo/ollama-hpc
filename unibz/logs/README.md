# Logs

This directory contains runtime logs generated by Slurm jobs.

Typical files include:
- `<JOBID>.out` — standard output
- `<JOBID>.err` — standard error

These files are generated at runtime and are intentionally **not tracked** in Git.
